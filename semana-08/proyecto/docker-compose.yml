services:
  mongo:
    image: mongo:7
    container_name: tienda-telecom-seguridad-mongo
    ports:
      - "27017:27017"
    volumes:
      - tienda-mongo-data:/data/db

volumes:
  tienda-mongo-data:
